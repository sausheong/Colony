- unless @user.requests.empty?
  .icons.icons_add_friend
  %a{:href => '/requests/pending'}    
    You have #{@user.requests.size} friend request(s). 
- else
  No pending requests
%hr.space  
- unless @user.pending_events.empty?
  .icons.icons_pending_events
  %a{:href => '/events'}    
    You have #{@user.pending_events.size} pending event(s). 
- else
  No pending event